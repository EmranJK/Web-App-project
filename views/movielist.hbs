{{> menu}}

<section class="ui center aligned middle aligned segment" style="background: radial-gradient(ellipse at top, #e66465, transparent),
            radial-gradient(ellipse at bottom, #4d9f0c, transparent);">
  <h2 class="ui header">
    {{movielist.title}}
  </h2>
  {{> listmovies}}
  {{> addmovie}}
</section>